<Page x:Class="DataBindingWPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DataBindingWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--<Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>-->

        <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Width="230">
            <Label Content="Текущий пользователь:"/>
            <WrapPanel Orientation="Horizontal">
                <Label Content="ID:"></Label>
                <Label x:Name="CurrentId" Content="{Binding CurrentDoctor.Id}"></Label>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Content="Фамилия:"></Label>
                <Label x:Name="CurrentName" Content="{Binding CurrentDoctor.Name}"></Label>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Content="Имя:"></Label>
                <Label x:Name="CurrentLastName" Content="{Binding CurrentDoctor.LastName}"></Label>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Content="Отчество:"></Label>
                <Label x:Name="CurrentMiddleName" Content="{Binding CurrentDoctor.MiddleName}"></Label>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Content="Специальность:"></Label>
                <Label x:Name="CurrentSpecialisation" Content="{Binding CurrentDoctor.Specialisation}"></Label>
            </WrapPanel>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" Width="400">
            <WrapPanel Orientation="Horizontal">
                <Button Margin="5,0,0,0" Width="120" Click="toAddPatient">Создать пациента</Button>
                <Button Margin="5,0,0,0" Width="100" Click="toAppointment">Начать прием</Button>
                <Button Margin="5,0,0,0" Width="150" Click="toChangeInfo">Изменить информацию</Button>
            </WrapPanel>
            <ListView ItemsSource="{Binding Patients}" Margin="0,10,0,0" Height="400" x:Name="List">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Идентификатор: "/>
                                <TextBlock Text="{Binding Id}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Фамилия: "/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Имя: "/>
                                <TextBlock Text="{Binding LastName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Отчество: "/>
                                <TextBlock Text="{Binding MiddleName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Дата рождения: "/>
                                <TextBlock Text="{Binding Birthday}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Номер телефона: "/>    
                                <TextBlock Text="{Binding Phone}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
