<Page x:Class="DataBindingWPF.Pages.AppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DataBindingWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Appointment">

    <Grid>
      
<<<<<<< HEAD
            <StackPanel Width="230" HorizontalAlignment="Left">
                <Label Content="Индентификатор врача"></Label>
                <TextBox x:Name="_doctorID" Text="{Binding Appointment.Doctor_id}"></TextBox>
                <Label Content="Дата приема"></Label>
            <DatePicker SelectedDate="{Binding Appointment.Date}"></DatePicker>
                <Label Content="Диагноз"></Label>
            <TextBox x:Name="_diagnosis" Text="{Binding Appointment.Diagnosis}"></TextBox>
                <Label Content="Рекомендации"></Label>
            <TextBox x:Name="_recomendation" Text="{Binding Appointment.Recomendations}"></TextBox>
                <Button Margin="0,10,0,0" Click="addAppointment">Записать</Button>
            <Button Margin="0,10,0,0" Click="Back">Назад</Button>
        </StackPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Name="lolo">
            <Label Content="История приемов пациента">
                
            </Label>
            <ListView ItemsSource="{Binding AppointmentStories}" Margin="0,10,0,0" Height="400" Width="400" x:Name="List" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Дата приема: "/>
                                <TextBlock Text="{Binding Date}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Индентификатор врача: "/>
                                <TextBlock Text="{Binding Doctor_id}"/>
                        </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ФИО врача: "/>
                                <TextBlock Text="{Binding Doctor_Fio}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Диагноз: "/>
                                <TextBlock Text="{Binding Diagnosis}"/>
                        </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Рекомендации: "/>
                                <TextBlock Text="{Binding Recomendations}"/>
                            </StackPanel>
                        </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        </StackPanel>
=======
            <StackPanel Width="230">
                <Label Content="Индентификатор врача"></Label>
                <TextBox x:Name="_doctorID" Text="{Binding Doctor_id}"></TextBox>
                <Label Content="Дата приема"></Label>
                <DatePicker SelectedDate="{Binding Date}"></DatePicker>
                <Label Content="Диагноз"></Label>
                <TextBox x:Name="_diagnosis" Text="{Binding Diagnosis}"></TextBox>
                <Label Content="Рекомендации"></Label>
                <TextBox x:Name="_recomendation" Text="{Binding Recomendations}"></TextBox>
                <Button Margin="0,10,0,0" Click="addAppointment">Записать</Button>
            </StackPanel>
   
>>>>>>> 68bfe7522069bb4544432cb3b01b2ae64a25edc3
    </Grid>
</Page>
